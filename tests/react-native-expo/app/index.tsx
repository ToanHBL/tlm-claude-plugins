import UserListScreen from '@/_modules/screens/User/UserListScreen';

// Routing only (≤5 lines) — all business logic lives in the Screen (ai/reactnative/03).
export default function Page() {
  return <UserListScreen />;
}
